# OpenAI API key (required for AI features)
OPENAI_API_KEY=your_openai_api_key_here

# API key for authenticating requests from Moodle (optional - if empty, auth is skipped for dev)
# Must match the API key configured in Moodle: Site Administration → Plugins → Activity modules → AI Writing Assistant
WRITEASSIST_API_KEY=
